# Таблица (table)

**Назначение:**
Компонент для отображения табличных данных.

**Пропсы:**
- `columns` (array): колонки
- `data` (array): данные

**Состояния:**
- Обычная
- С сортировкой
- С фильтрацией 