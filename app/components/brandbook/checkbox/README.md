# Чекбоксы (checkbox)

**Назначение:**
Компонент для выбора одного или нескольких вариантов.

**Пропсы:**
- `checked` (boolean): выбран
- `label` (string): подпись

**Состояния:**
- Обычный
- Выбран
- Отключён 